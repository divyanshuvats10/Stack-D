const Loader = () => (
	<div className="mx-auto flex min-h-[40vh] max-w-7xl items-center justify-center px-5">
		<div className="flex items-center gap-3 font-mono text-xs uppercase tracking-[0.18em] text-muted">
			<span className="h-3 w-3 animate-pulse rounded-full bg-tomato" /> Loading fresh data
		</div>
	</div>
);
export default Loader;